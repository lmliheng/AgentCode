检索结果非常糟糕


P0：改分块策略（影响最大）
按 markdown heading 分块，每个 ## section 作为一个 chunk（或进一步拆分），继承标题路径作为前缀，代码块保持完整不劈开。这一步改完，检索质量会有质的提升。

P1：提升 embedding 维度
256 → 1024 或 2048。智谱 embedding-3 支持更高维度，当前 256 太低，语义挤在一起区分不开。

P2：清理重复数据
jsx 目录和其他目录的重复内容需要去掉，否则同一份数据被索引两遍，检索结果被噪声淹没。